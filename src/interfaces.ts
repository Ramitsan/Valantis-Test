export interface ICardData {
  brand: string,
  id: string,
  price: number,
  product: string
}

export interface ICardFields {
  brand: Array<string>, 
  product: Array<string>, 
  price: Array<number>
}